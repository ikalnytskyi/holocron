sudo: false
language: python
python: 3.5

install:
  - pip install tox

script:
  - tox

after_success:
  - pip install coveralls
  - coverage run --source=holocron setup.py test
  - coveralls

notifications:
  email: false
